interface WindowInfo {
    name: string;
    id: string;
    state: string;
    stateEx: "closed" | "minimized" | "hidden" | "normal" | "maximized";
    isVisible: boolean;
    left: number;
    top: number;
    width: number;
    height: number;
}
interface OwWindow extends WindowInfo {
    id: string;
    minimize: () => void;
    maximize: () => void;
    restore: () => void;
    close: () => void;
}
export declare const useWindow: (name: string, { displayLog }: {
    displayLog?: boolean | undefined;
}) => readonly [OwWindow | undefined];
export {};
