import { Constructor } from "../common/types";
declare const addActions: (actionsFetcher: (self: any) => object) => <T extends Constructor<{}>>(BaseClass: T) => {
    new (...args: any[]): {};
} & T;
export { addActions };
