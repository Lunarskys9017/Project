import { Constructor } from "../common/types";
declare const revertible: (propsToWatch: string[]) => <T extends Constructor<{}>>(BaseClass: T) => {
    new (...args: any[]): {};
} & T;
export { revertible };
